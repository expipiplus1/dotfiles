diff --git a/autoload/neomake.vim b/autoload/neomake.vim
index ad5e5c5..2c6ccbf 100644
--- a/autoload/neomake.vim
+++ b/autoload/neomake.vim
@@ -1073,13 +1073,11 @@ function! s:AddExprCallback(jobinfo, prev_index) abort
                 call remove(list, k)
             endfor
         endif
-        if file_mode
-            call setloclist(0, list, 'r')
-        else
-            call setqflist(list, 'r')
-        endif
+        call setqflist(list, 'r')
     endif
 
+    call lightline#update()
+
     return entries
 endfunction
 
