diff --git a/autoload/startify.vim b/autoload/startify.vim
index 54febfc..8da74b5 100644
--- a/autoload/startify.vim
+++ b/autoload/startify.vim
@@ -837,6 +837,11 @@ function! s:cd_to_vcs_root(path) abort
       return
     endif
   endfor
+  let s = system("git rev-parse --show-toplevel")
+  if !v:shell_error
+    execute 'cd' . s
+    return
+  endif
 endfunction
 
 " Function: s:close {{{1
